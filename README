# ws-video-recorder
Record video streaming via websocket implemented by nodejs.

## How to use

* `npm install`
* edit the websocket path in `recorder.js`
* `node recorder.js`

Compatible to node 16.13.2.
